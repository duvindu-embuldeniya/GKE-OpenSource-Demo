apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.ws.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.ws.name }}
  ports:
    - port: 3002
      targetPort: 3002